---
interface Props {
  urls: string[]
}
const { urls } = Astro.props
---
 
<div>
  {
    urls.map((url) => (
      <img src={url} alt="Random Dog" style="max-width:200px;height:auto;" />
    ))
  }
</div>
